import classes from '../../styles/components/footer.module.scss';

const Footer = () => {
	return (
		<footer className={classes.footerContainer}>
			<p className={classes.author}>@Created by XYZ</p>
		</footer>
	);
};

export default Footer;
